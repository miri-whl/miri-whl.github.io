{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://miri-whl.github.io/schemas/scoring-v1.json",
  "title": "Miri Scoring Configuration",
  "description": "Declares how a linter computes the two scores in a lint report: the ratio-based Conformance score (MUST-gated) and the severity-graded Health score. The Conformance model is a RATIO, not a bare total: a check whose condition does not apply, or which a linter forfeits for lack of a declared capability, leaves BOTH the numerator and the denominator. An earlier wording here called it 'the additive model: sum of passing check weights (0-100)', which contradicted both producer checklists and check-v1.json's definition of `conditional` — a stale description that survived the 0.5.0 semantics correction because nothing reads this file. Draft: reconcile with the reference linter before pinning constraints.",
  "type": "object",
  "required": ["schema_version", "conformance", "health"],
  "properties": {
    "schema_version": {"type": "string"},
    "conformance": {
      "type": "object",
      "description": "Score = sum of the weights of PASSING checks, over the sum of the weights of checks that APPLIED and could be evaluated. Excluded and forfeited checks leave both sides, so the denominator is at most 100 and two scores are comparable only alongside their denominators. This is the not-applicable-is-not-a-pass rule: a check whose condition does not hold has not been satisfied, and awarding its weight would let an artifact earn points for an absence. GATES: a check with `scoring: gate` carries weight 0 and is in NEITHER sum. Do not infer that a failing gate is free - by this formula a weight-0 check contributes 0/0 whether it passed or failed, so the arithmetic cannot see it. A failing gate makes the artifact NON-CONFORMING, and the only place that is representable is `must_failures` in lint-report-v1, where it is coupled to the grade and the 74 cap. A linter computing conformance from this formula alone, without populating `must_failures`, will report a wheel with a corrupt RECORD as conforming. THERE IS NO COVERAGE FLOOR, deliberately — a small denominator usually means a simpler artifact rather than a worse one, and withholding a grade for having less surface would penalize simplicity. A linter MUST NOT impose a floor of its own: one linter with a floor and one without produce incomparable grades, which is the one thing a score exists to prevent.",
      "required": ["must_failure_cap", "grade_bands"],
      "properties": {
        "report_effective_denominator": {
          "type": "boolean",
          "description": "Whether the linter reports the effective denominator and the excluded/forfeited counts beside the score. Both producer checklists make this a MUST, because a score without its denominator is not comparable to another score. Declared here so a scoring configuration cannot silently omit it; the report fields themselves live in lint-report-v1.json."
        },
        "gate_failure_is_non_conforming": {"type": "boolean", "description": "Whether the implementation treats a failing `scoring: gate` check as non-conformance. There is no legitimate reason to set this false; it exists so a configuration must state the behaviour rather than leave it inferred from a weight of 0."},
        "must_failure_cap": {
          "type": "integer",
          "description": "Score ceiling when any MUST check FAILS (non-conforming); the standard fixes this at 74. This concerns a MUST that failed, never one excluded or forfeited: an excluded MUST was not an obligation for this artifact, and a forfeited MUST leaves conformance undetermined rather than capped (see both producer checklists' Scoring Model)."
        },
        "grade_bands": {
          "type": "object",
          "description": "Lower bound of each grade band",
          "properties": {
            "gold": {"type": "integer"},
            "silver": {"type": "integer"},
            "bronze": {"type": "integer"}
          },
          "additionalProperties": false
        },
        "gold_requires_provenance": {
          "type": "boolean",
          "description": "Whether Gold additionally requires PEP 740 attestations (MIRI-PY-005) for public-index wheels"
        }
      },
      "additionalProperties": true
    },
    "health": {
      "type": "object",
      "description": "The normative-baseline severity-graded model over canonical per-check severities",
      "required": ["method"],
      "properties": {
        "method": {
          "type": "string",
          "description": "Named scoring method (e.g. baseline additive-penalty, or a tiered-penalty variant)"
        },
        "profile": {
          "type": "string",
          "description": "Frozen, versioned severity profile identifier (e.g. balanced@1)"
        },
        "parameters": {
          "type": "object",
          "description": "Method parameters (severity weights, escalation factors, per-severity caps)",
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
