{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://miri-whl.github.io/schemas/agent-event-v1.json",
  "title": "Miri Standard Agent Integration Event",
  "description": "The request a host sends to an agent-integration binding when a trigger fires (Agent Integration Contract §3.2). The response is a discovery-envelope-v1 document carrying a `findings` payload key.\n\nNote the deliberate asymmetry with discovery-envelope-v1.json, whose root is OPEN. That root is open because a new operation adds a payload key and a closed root would forbid it on the day it lands. This root is CLOSED for the opposite reason: the contract's §3.2 forbids an event carrying a file path, a diff, a buffer, or the user's source, and a closed object is what makes that prohibition mechanical rather than advisory. A host that cannot express its event without adding a field is a host reaching for the user's code.",
  "type": "object",
  "required": ["schema_version", "kind", "phase", "subject"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Wire-schema version of this event. An integer string — \"1\", \"2\" — compared numerically, matching every other schema_version in the standard."
    },
    "kind": {
      "type": "string",
      "enum": [
        "dependency.add",
        "dependency.version_change",
        "package.replaced",
        "package.first_reference",
        "integration.begin",
        "runtime.error",
        "test.author"
      ],
      "description": "The observable moment, from the closed set in Agent Integration Contract §3.1. Closed for the same reason the servable set and the error codes are closed: two bindings that disagree about what an event is do not interoperate. `package.replaced` fires when an installed artifact is replaced without its version changing — the case a purl cannot distinguish, because a purl identifies a name and a version and not a byte stream (Lifecycle and Security Metadata §9.6)."
    },
    "phase": {
      "type": "string",
      "enum": ["before", "after"],
      "description": "Whether the action that produced the event has landed. `runtime.error` is necessarily `after`. `dependency.add` and `package.replaced` are `after` too - the artifact must be installed for there to be anything to read. Every other kind is `before`."
    },
    "subject": {
      "type": "object",
      "required": ["package"],
      "additionalProperties": false,
      "description": "What the event is about, and nothing else. Closed by design: §3.2 forbids a file path, a diff, a buffer or the user's source, and a binding that needs the user's code to decide what to say has left this standard's scope and entered the host's.",
      "properties": {
        "package": {
          "type": "string",
          "description": "The name the event is about, as observed. Its grammar depends on `package_kind`, which says which kind of name this is — see that field. An import name matches the Discovery Contract §3.0 grammar and can be passed to an operation without re-validation; a distribution name cannot, and the consumer joins it when it can."
        },
        "package_kind": {
          "type": "string",
          "enum": ["import", "distribution"],
          "default": "import",
          "description": "Which kind of name `package` carries. A manifest edit yields a DISTRIBUTION name — `miri-py`, `python-dateutil`, `greet-adversarial` — and those are not valid import names; the mapping between them lives in the installed distribution's own metadata, so for a package that is not yet installed the two cannot be joined at all. Requiring an import name here made the REQUIRED `dependency.add` mapping in the Claude Code binding unsatisfiable for most real packages, including the fixture this standard's own A13 golden is built on. A consumer joins the two where it can and reports honestly where it cannot, which is the same honest-degradation posture the Consumption Map applies everywhere else."
        },
        "version": {
          "type": "string",
          "description": "A version or version specifier, where the kind implies one. Carried as written; this contract does not parse it."
        },
        "symbol": {
          "type": "string",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*(\\.[A-Za-z_][A-Za-z0-9_]*)*$",
          "description": "A dotted qualified name, where the kind implies one (runtime.error naming a failing surface, test.author naming the surface under test)."
        }
      }
    }
  },
  "allOf": [
    {
      "$comment": "Agent Integration Contract §3.2 — runtime.error is necessarily `after`; it reports something that has already happened.",
      "if": { "properties": { "kind": { "const": "runtime.error" } }, "required": ["kind"] },
      "then": { "properties": { "phase": { "const": "after" } } }
    },
    {
      "$comment": "§3.2 — only `runtime.error` and `dependency.add` may be `after`. runtime.error reports something that has happened; dependency.add is `after` because a not-yet-installed package is not readable by a local surface. Every other kind fires while its action is still a proposal. This was found by an implementer building the first binding: the schema previously required every REQUIRED kind to be `before` and so rejected the only behavior a local surface can produce.",
      "if": { "properties": { "phase": { "const": "after" } }, "required": ["phase"] },
      "then": { "properties": { "kind": { "enum": ["runtime.error", "dependency.add"] } } }
    },
    {
      "$comment": "§3.2 — an import name must match the Discovery Contract §3.0 grammar, so it can be passed to an operation without re-validation. A distribution name is constrained per PEP 503 instead, which is what a manifest actually contains.",
      "if": { "properties": { "subject": { "properties": { "package_kind": { "const": "distribution" } }, "required": ["package_kind"] } }, "required": ["subject"] },
      "then": { "properties": { "subject": { "properties": { "package": { "pattern": "^[A-Za-z0-9]([A-Za-z0-9._-]*[A-Za-z0-9])?$" } } } } }
    },
    {
      "$comment": "§3.2 — the default. Where package_kind is absent or `import`, the name must be a valid import name.",
      "if": { "not": { "properties": { "subject": { "properties": { "package_kind": { "const": "distribution" } }, "required": ["package_kind"] } }, "required": ["subject"] } },
      "then": { "properties": { "subject": { "properties": { "package": { "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" } } } } }
    },
    {
      "$comment": "§3.1 — dependency.version_change is about a version, so it carries one. Without this a binding could fire the kind with nothing to distinguish it from dependency.add.",
      "if": { "properties": { "kind": { "const": "dependency.version_change" } }, "required": ["kind"] },
      "then": { "properties": { "subject": { "required": ["package", "version"] } } }
    }
  ]
}
